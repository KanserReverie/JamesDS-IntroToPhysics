using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Dumpling.Core
{
    /// <summary> This is the abstract class that all modular components will  </summary>
    public abstract class DumplingModularComponent : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}